## 🎯 **第一阶段：需求分析与技术选型（1-2轮对话）**

### **AI对话指令模板1：项目定义**
```markdown
# 项目需求说明书

## 项目名称
在线答题系统 v3.0（从零重构版）

## 核心需求
基于对原项目（Flask答题网站）的功能分析，我需要一个**架构清晰、模块化、易扩展**的答题系统，具体要求如下：

### 1. 核心功能模块
- **用户系统**：学生答题、成绩查看
- **试卷管理**：科目/等级分类、试卷列表
- **试题解析**：Word文档上传、AI/传统双解析器、图片提取
- **答题判卷**：多题型支持（单选/多选/判断/填空/主观/编程）、自动判卷
- **后台管理**：科目/等级管理、成绩统计、题目编辑

### 2. 技术要求
- **后端**：Python Flask + SQLAlchemy ORM
- **数据库**：SQLite（开发）/PostgreSQL（生产）
- **前端**：Bootstrap 5 + Jinja2 模板
- **AI集成**：支持DeepSeek/OpenAI等多提供商
- **架构模式**：清晰的MVC或分层架构

### 3. 质量要求
- 单个函数不超过50行
- 模块化设计，高内聚低耦合
- 完整的类型注解和文档字符串
- 统一的错误处理机制
- 易于扩展新题型或解析器

## 请帮我：
1. 推荐合适的技术栈组合
2. 设计项目目录结构（符合Python最佳实践）
3. 制定分阶段实施计划（从小到大，渐进式）
```

---

## 🏗️ **第二阶段：架构设计与模块划分（2-3轮对话）**

### **AI对话指令模板2：架构设计**
```markdown
# 架构设计请求

## 基于上一轮讨论，请帮我设计具体架构：

### 1. 分层架构设计
请设计清晰的层次结构，例如：
- **数据层** (models/) - SQLAlchemy模型
- **服务层** (services/) - 业务逻辑封装
- **解析层** (parsers/) - Word解析器、AI解析器
- **工具层** (utils/) - 通用工具函数
- **视图层** (routes/) - Flask路由
- **模板层** (templates/) - Jinja2模板

### 2. 模块职责划分
请定义以下模块的具体职责：
- `models/`：数据库模型（Subject, Level, Exam, Question, Submission, Answer）
- `services/exam_service.py`：试卷上传、解析、保存逻辑
- `services/question_service.py`：题目处理、判卷逻辑
- `parsers/word_parser.py`：传统Word文档解析器
- `parsers/ai_parser.py`：AI智能解析器
- `utils/image_utils.py`：图片处理工具
- `utils/question_utils.py`：题目类型检测、格式化工具

### 3. 接口设计
请设计关键接口，例如：
```python
# 解析器接口
class BaseParser:
    def parse(self, file_path: str) -> List[Dict]:
        """解析文档，返回题目列表"""
        pass

# 判卷器接口  
class BaseGrader:
    def grade(self, student_answer: str, correct_answer: str) -> Tuple[float, bool]:
        """判卷，返回得分和是否正确"""
        pass
```

## 输出要求：
1. 详细的项目结构树
2. 每个主要模块的接口定义
3. 模块间的依赖关系图（文字描述）
4. 数据库ER图（文字描述）
```

---

## 🔨 **第三阶段：逐步实现（多个小任务）**

### **策略：每次只实现一个小模块，立即验证**

#### **子任务1：数据库模型（第1轮）**
```markdown
# 实现数据库模型

请根据以下ER关系实现SQLAlchemy模型：

## 表结构
1. **Subject** (id, name, created_at)
2. **Level** (id, name, created_at)
3. **Exam** (id, title, subject_id, level_id, created_at)
4. **Question** (id, exam_id, type, text, options(JSON), answer, points, order, has_image, image_data)
5. **Submission** (id, exam_id, student_name, submit_time, total_score)
6. **Answer** (id, submission_id, question_id, student_answer, score, is_correct)

## 要求：
1. 使用`db.Model`基类
2. 定义完整的关系（一对多、多对多）
3. 为Question.options使用JSON类型
4. 为所有字段添加类型注解
5. 在`models/__init__.py`中导出所有模型

## 完成后请：
1. 提供创建数据库的代码片段
2. 验证模型关系是否正确
```

#### **子任务2：工具函数模块（第2轮）**
```markdown
# 实现工具函数模块

请实现以下工具函数，确保每个函数职责单一：

## 文件：`utils/question_utils.py`
1. `detect_question_type(text: str) -> str`：根据文本推断题型
2. `normalize_answer(answer: str, question_type: str) -> str`：标准化答案（特别处理判断题）
3. `format_options(options_data) -> List[Dict]`：统一格式化选项

## 文件：`utils/image_utils.py`
1. `get_image_type(filename: str) -> str`：根据扩展名返回图片类型
2. `validate_image_size(data: bytes, max_size: int) -> bool`：验证图片大小
3. `extract_base64_from_docx(file_path: str) -> List[Dict]`：从Word提取图片（可后续实现）

## 要求：
- 每个函数不超过30行
- 完整的文档字符串（参数、返回、示例）
- 独立的单元测试建议
```

#### **子任务3：Word解析器（第3轮）**
```markdown
# 实现基础Word解析器

请实现`parsers/word_parser.py`，包含：

## 核心函数
```python
class WordDocumentParser:
    def __init__(self):
        self.logger = logging.getLogger(__name__)
    
    def parse(self, file_path: str) -> Dict[str, Any]:
        """解析Word文档，返回结构化的试卷数据"""
        # 实现步骤：
        # 1. 读取Word文档段落
        # 2. 检测封面页并跳过
        # 3. 按题型分割题目区域
        # 4. 解析每道题目（文本、选项、答案）
        # 5. 返回题目列表和总分数
```

## 解析规则（基于原项目）：
1. **题型识别**：通过"一、单选题"等标题识别
2. **题目编号**：支持"1."、"1、"、"（1）"等格式
3. **选项格式**：A. 内容 或 A、内容
4. **答案格式**："正确答案：A" 或 "答案：正确"

## 要求：
- 函数拆分为小函数（detect_cover_page, parse_section等）
- 处理常见边界情况
- 提供解析示例Word的测试代码
```

---

## ✅ **第四阶段：集成与测试（2-3轮对话）**

### **AI对话指令模板3：集成测试**
```markdown
# 集成测试请求

现在已实现以下模块：
- 数据库模型（models/）
- 工具函数（utils/）
- Word解析器（parsers/word_parser.py）

## 请帮我：
1. **创建Flask应用骨架**：`app/__init__.py`，配置数据库、日志、蓝图
2. **实现第一个API**：试卷上传接口 `/api/exams/upload`
   - 接收Word文件
   - 调用解析器
   - 保存到数据库
3. **编写集成测试**：
   ```python
   def test_upload_exam():
       # 上传示例Word文档
       # 验证解析结果
       # 验证数据库记录
   ```

## 测试数据：
使用项目中的示例文档：`案例题目/202403Python二级真题(带答案解析).docx`

## 输出要求：
1. 完整的Flask应用初始化代码
2. 至少一个可工作的API端点
3. 验证步骤和预期结果
```

---

## 📋 **关键对话技巧**

### **1. 明确边界，避免范围蔓延**
```markdown
❌ "帮我重写整个项目"
✅ "本次对话只实现Word文档解析模块，不包括用户认证和前端页面"
```

### **2. 要求具体输出格式**
```markdown
请提供：
1. 完整的代码文件（包含导入语句和函数定义）
2. 使用示例（如何调用这个函数）
3. 测试用例建议（输入、预期输出）
4. 已知限制和边界情况
```

### **3. 分步确认，避免大段重构**
```markdown
**我的策略**：
1. 先确认这个模块的设计方案
2. 你实现核心函数（50行以内）
3. 我验证功能是否正常
4. 你补充边缘情况和测试
5. 进入下一个模块
```

---

## 🚀 **推荐的重写顺序（从简到繁）**

### **顺序建议：**
```
第1周：基础框架
  ├── Day 1: 项目骨架 + 数据库模型 ✅
  ├── Day 2: 工具函数模块 ✅
  ├── Day 3: Word解析器（基础版） ✅
  └── Day 4: Flask应用初始化 + 第一个API ✅

第2周：核心业务逻辑
  ├── Day 5: 试卷管理服务
  ├── Day 6: 题目判卷服务
  ├── Day 7: AI解析器集成
  └── Day 8: 图片提取与处理

第3周：用户界面
  ├── Day 9: 学生答题页面
  ├── Day 10: 成绩展示页面
  ├── Day 11: 后台管理界面
  └── Day 12: 响应式优化

第4周：高级功能
  ├── Day 13: 错误处理与日志
  ├── Day 14: 性能优化
  ├── Day 15: 部署配置
  └── Day 16: 文档编写
```

---

## ⚠️ **避免重蹈覆辙的关键点**

### **1. 坚持小函数原则**
```python
# ❌ 避免
def parse_entire_exam():  # 800行函数
    # 所有逻辑混在一起
    
# ✅ 坚持
def parse_exam(file_path):
    paragraphs = read_document(file_path)
    cover_end = detect_cover_page(paragraphs)
    sections = split_by_question_type(paragraphs, cover_end)
    return parse_sections(sections)  # 每个函数<50行
```

### **2. 强制类型注解和文档**
```python
def detect_question_type(text: str) -> str:
    """
    根据题目文本自动检测题型
    
    Args:
        text: 题目文本内容
        
    Returns:
        题型标识字符串，如 'choice', 'judge'等
        
    Raises:
        ValueError: 当文本为空时
    """
    if not text:
        raise ValueError("题目文本不能为空")
    # 具体实现...
```

### **3. 每个模块独立可测试**
```
project/
├── tests/              # 测试目录
│   ├── test_models.py
│   ├── test_parsers.py
│   └── test_services.py
├── requirements-dev.txt  # 开发依赖
└── pytest.ini          # 测试配置
```

### **4. 版本控制策略**
```bash
# 每次完成一个小功能就提交
git add .
git commit -m "feat: 实现Word基础解析器（模块化设计）"

# 使用功能分支
git checkout -b feature/word-parser
# 开发完成后合并
git checkout main
git merge --no-ff feature/word-parser
```

---

## 📞 **开始你的重写旅程**

### **你的第一轮对话可以这样开始：**
```markdown
# 从零重写答题网站 - 第1轮：项目定义

你好，我需要从零重写一个在线答题系统。原项目是一个Flask应用，但代码质量较差。

## 我的核心需求：
1. 支持Word文档上传和解析（含图片）
2. 支持多种题型（单选/多选/判断/填空/主观/编程）
3. 自动判卷功能
4. 科目和等级分类管理
5. 成绩统计和查询

## 我对新项目的要求：
- 采用Python Flask + SQLAlchemy
- 清晰的MVC或分层架构
- 每个函数不超过50行
- 完整的类型注解和文档
- 易于扩展和维护

## 请帮我：
1. 推荐一个合理的项目结构（目录树）
2. 设计数据库模型（ER关系）
3. 制定一个渐进式实现计划（从最简单的开始）
```

**记住**：好的重构是**对话**，不是**指令**。每次只让AI做一件小事，验证通过后再继续。这样即使某个部分出现问题，也容易回滚和调整。

需要我帮你开始第一轮具体的对话设计吗？